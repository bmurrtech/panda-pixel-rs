{
  "name": "panda-pixel-rs-web",
  "version": "0.1.0-alpha",
  "private": true,
  "scripts": {
    "build:css": "tailwindcss -i ./src/style.css -o ./public/app.css --minify"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.0"
  }
}
